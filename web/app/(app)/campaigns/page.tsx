'use client';
import { Card } from '@/components/ui/Card';
export default function Campaigns() {
  return (
    <Card>
      <div className="text-ink-muted">Campaigns history — v1.1. The dashboard already lists recent analyses.</div>
    </Card>
  );
}
